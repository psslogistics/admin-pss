"use client";

import Link from "next/link";
import { usePathname } from "next/navigation";
import { useEffect, useMemo, useRef, useState } from "react";
import { Bell, ChevronRight, Menu, Moon, PanelLeftIcon, Search, Sun, X } from "lucide-react";
import { cn } from "@/lib/utils";
import { PssIcon } from "@/components/ui/icon";
import { navigationGroups } from "@/components/navigation/navigation-config";
import { searchEmployeeMaster } from "@/lib/employee-search";
import { useEmployee } from "@/components/dashboard/employee-provider";

function Brand({ compact = false }: { compact?: boolean }) { return <div className={cn("flex items-center gap-2.5", compact && "justify-center")}><div className="grid size-8 shrink-0 place-items-center rounded-lg bg-primary text-sm font-bold text-primary-foreground shadow-xs">P</div>{!compact && <span className="text-[15px] font-semibold tracking-tight">PSS Logistics</span>}</div>; }
function Avatar({ initials, small = false }: { initials: string; small?: boolean }) { return <div className={cn("grid shrink-0 place-items-center rounded-full bg-primary/10 font-semibold text-primary", small ? "size-8 text-xs" : "size-11 text-sm")}>{initials}</div>; }

export default function EmployeeShell({ children }: { children: React.ReactNode }) {
  const pathname = usePathname();
  const { profile, selectEmployee, resetDemo, can, notifications } = useEmployee();
  const [sidebarOpen, setSidebarOpen] = useState(false);
  const [collapsed, setCollapsed] = useState(false);
  const [profileOpen, setProfileOpen] = useState(false);
  const [searchOpen, setSearchOpen] = useState(false);
  const [searchQuery, setSearchQuery] = useState("");
  const [theme, setTheme] = useState<"light" | "dark">("light");
  const [themeReady, setThemeReady] = useState(false);
  const profileRef = useRef<HTMLDivElement>(null);
  const searchRef = useRef<HTMLInputElement>(null);
  const visibleGroups = useMemo(() => navigationGroups.map((group) => ({ ...group, items: group.items.filter((item) => can(item.permission)) })).filter((group) => group.items.length), [can]);
  const visibleItems = useMemo(() => visibleGroups.flatMap((group) => group.items), [visibleGroups]);
  const currentRoute = visibleItems.find((item) => item.href === pathname || (item.href === "/dashboard/myClients" && pathname.startsWith(item.href)))?.title ?? "Workspace";
  const searchResults = useMemo(() => searchEmployeeMaster(searchQuery, profile), [searchQuery, profile]);
  const unreadCount = notifications.filter((item) => !item.read).length;

  useEffect(() => { const saved = localStorage.getItem("pss-theme"); const next = saved === "dark" || saved === "light" ? saved : window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light"; document.documentElement.classList.toggle("dark", next === "dark"); const timer = window.setTimeout(() => { setTheme(next); setThemeReady(true); }, 0); return () => window.clearTimeout(timer); }, []);
  useEffect(() => { if (!profileOpen) return; const close = (event: MouseEvent) => { if (profileRef.current && !profileRef.current.contains(event.target as Node)) setProfileOpen(false); }; document.addEventListener("mousedown", close); return () => document.removeEventListener("mousedown", close); }, [profileOpen]);
  useEffect(() => { if (!searchOpen) return; searchRef.current?.focus(); const close = (event: KeyboardEvent) => { if (event.key === "Escape") setSearchOpen(false); }; window.addEventListener("keydown", close); return () => window.removeEventListener("keydown", close); }, [searchOpen]);
  useEffect(() => { const shortcut = (event: KeyboardEvent) => { if (event.key.toLowerCase() === "k" && (event.metaKey || event.ctrlKey)) { event.preventDefault(); setSearchOpen((value) => !value); } }; window.addEventListener("keydown", shortcut); return () => window.removeEventListener("keydown", shortcut); }, []);
  const toggleTheme = () => { const next = theme === "dark" ? "light" : "dark"; setTheme(next); document.documentElement.classList.toggle("dark", next === "dark"); localStorage.setItem("pss-theme", next); };

  return <div className="min-h-svh bg-background text-foreground">
    {sidebarOpen && <button aria-label="Close navigation" className="fixed inset-0 z-30 bg-black/30 backdrop-blur-[2px] md:hidden" onClick={() => setSidebarOpen(false)} />}
    <aside className={cn("fixed inset-y-0 left-0 z-40 flex w-64 flex-col border-r border-sidebar-border/70 bg-sidebar text-sidebar-foreground transition-[width,transform] duration-200 ease-linear motion-reduce:transition-none md:translate-x-0", collapsed && "md:w-12", sidebarOpen ? "translate-x-0" : "-translate-x-full")}>
      <div className={cn("flex h-14 items-center border-b border-sidebar-border/50 px-4", collapsed && "md:justify-center md:px-2")}><Brand compact={collapsed} /><button aria-label="Close navigation" className="ml-auto rounded-lg p-1.5 hover:bg-sidebar-accent md:hidden" onClick={() => setSidebarOpen(false)}><X className="size-4" /></button></div>
      <nav className="flex-1 overflow-y-auto px-2 py-4">{visibleGroups.map((group) => <div key={group.label} className="mb-5"><p className={cn("mb-2 px-3 text-[10px] font-semibold uppercase tracking-[0.16em] text-sidebar-foreground/40", collapsed && "md:hidden")}>{group.label}</p><div className="space-y-1">{group.items.map((item) => { const active = pathname === item.href || (item.href === "/dashboard/myClients" && pathname.startsWith(item.href)); return <Link key={item.href} prefetch={false} href={item.href} onClick={() => setSidebarOpen(false)} title={collapsed ? item.title : undefined} className={cn("flex h-9 items-center gap-3 rounded-lg px-3 text-sm transition-colors", active ? "bg-sidebar-primary/10 font-medium text-sidebar-primary" : "text-sidebar-foreground/65 hover:bg-sidebar-accent hover:text-sidebar-foreground", collapsed && "md:justify-center md:px-0")}><PssIcon name={item.icon} size="md" className="shrink-0" /><span className={cn(collapsed && "md:hidden")}>{item.title}</span>{active && !collapsed && <ChevronRight className="ml-auto size-3.5 opacity-50" />}</Link>; })}</div></div>)}</nav>
      <div className={cn("border-t border-sidebar-border/50 p-3", collapsed && "md:px-2")}><div className={cn("flex items-center gap-2.5 rounded-lg bg-sidebar-accent/60 p-2", collapsed && "md:justify-center md:bg-transparent md:p-0")}><Avatar initials={profile.initials} small /><div className={cn("min-w-0", collapsed && "md:hidden")}><p className="truncate text-xs font-medium">{profile.name}</p><p className="truncate text-[10px] text-sidebar-foreground/45">Authenticated employee</p></div></div></div>
    </aside>
    <div className={cn("min-h-svh transition-[padding] duration-200 md:pl-64", collapsed && "md:pl-12")}>
      <header className="sticky top-0 z-20 flex h-14 items-center gap-3 border-b border-border/70 bg-background/90 px-4 backdrop-blur-xl"><button aria-label="Open navigation" className="rounded-lg p-2 hover:bg-muted md:hidden" onClick={() => setSidebarOpen(true)}><Menu className="size-5" /></button><button aria-label="Toggle sidebar" className="hidden rounded-lg p-2 text-muted-foreground hover:bg-muted md:block" onClick={() => setCollapsed((value) => !value)}><PanelLeftIcon className="size-4" /></button><span className="shrink-0 text-sm font-semibold">{currentRoute}</span><button onClick={() => setSearchOpen(true)} aria-label="Search tasks, clients, shipments" className="ml-auto flex h-8 min-w-0 flex-1 items-center gap-2 rounded-lg border border-border/60 bg-muted/40 px-3 text-sm text-muted-foreground hover:bg-muted/60 sm:ml-8"><Search className="size-4 shrink-0 opacity-60" /><span className="truncate text-left">Search tasks, clients, shipments...</span><kbd className="ml-auto hidden shrink-0 text-[10px] sm:inline">⌘ K</kbd></button>{can("notifications.view") && <Link prefetch={false} href="/dashboard/notifications" aria-label={`Notifications${unreadCount ? `, ${unreadCount} unread` : ""}`} className="relative grid size-8 place-items-center rounded-lg text-muted-foreground hover:bg-muted hover:text-foreground"><Bell className="size-[18px]" />{unreadCount > 0 && <span className="absolute -right-1 -top-1 grid min-w-4 place-items-center rounded-full border-2 border-background bg-destructive px-1 text-[9px] text-destructive-foreground">{unreadCount}</span>}</Link>}<button aria-label={theme === "dark" ? "Switch to light mode" : "Switch to dark mode"} disabled={!themeReady} onClick={toggleTheme} className="grid size-8 place-items-center rounded-lg text-muted-foreground hover:bg-muted hover:text-foreground disabled:pointer-events-none">{theme === "dark" ? <Sun className="size-4" /> : <Moon className="size-4" />}</button><div className="relative" ref={profileRef}><button onClick={() => setProfileOpen((value) => !value)} aria-label="Open employee menu"><Avatar initials={profile.initials} small /></button>{profileOpen && <div className="absolute right-0 top-11 z-50 w-72 rounded-xl border border-border bg-popover p-1.5 shadow-lg"><div className="border-b border-border/60 px-3 py-2"><p className="text-sm font-medium">{profile.name}</p><p className="text-xs text-muted-foreground">{profile.role}</p></div><label className="block px-3 py-2 text-[10px] font-semibold uppercase tracking-wider text-muted-foreground">Frontend preview employee<select value={profile.id} onChange={(event) => { selectEmployee(event.target.value); setProfileOpen(false); }} className="mt-1.5 h-9 w-full rounded-lg border border-input bg-background px-2 text-xs text-foreground"><option value="rahul">Rahul Sharma · Operations</option><option value="priya">Priya Mehta · Support</option><option value="amit">Amit Shah · Limited operations</option></select></label><Link prefetch={false} href="/dashboard/profile" onClick={() => setProfileOpen(false)} className="mt-1 block rounded-lg px-3 py-2 text-sm hover:bg-accent">Profile</Link><button onClick={() => { resetDemo(); setProfileOpen(false); }} className="w-full rounded-lg px-3 py-2 text-left text-sm text-destructive hover:bg-destructive/10">Reset demo data</button></div>}</div></header>
      <main className="mx-auto min-w-0 w-full max-w-[1500px] p-4">{children}</main>
    </div>
    {searchOpen && <div className="fixed inset-0 z-50 flex items-start justify-center pt-[20vh]" onClick={() => setSearchOpen(false)}><div className="absolute inset-0 bg-background/60 backdrop-blur-sm" /><div className="relative mx-4 w-full max-w-lg overflow-hidden rounded-xl border border-border bg-popover shadow-2xl" onClick={(event) => event.stopPropagation()}><div className="flex h-12 items-center gap-3 border-b border-border/60 px-4"><Search className="size-[18px] text-muted-foreground" /><input ref={searchRef} value={searchQuery} onChange={(event) => setSearchQuery(event.target.value)} placeholder="Search visible workspace records..." className="flex-1 bg-transparent text-sm outline-none placeholder:text-muted-foreground/60" /><kbd className="text-[10px] text-muted-foreground">ESC</kbd></div><div className="max-h-80 overflow-y-auto p-2">{!searchQuery.trim() ? <p className="p-4 text-center text-xs text-muted-foreground">Search only includes pages and clients available to {profile.name}.</p> : searchResults.length ? searchResults.map((result) => <Link prefetch={false} key={result.id} href={result.href} onClick={() => { setSearchOpen(false); setSearchQuery(""); }} className="flex items-start gap-3 rounded-lg px-3 py-2.5 transition-colors hover:bg-accent"><Search className="mt-0.5 size-4 shrink-0 text-primary" /><span className="min-w-0"><span className="flex items-center gap-2 text-xs font-semibold"><span className="truncate">{result.title}</span><span className="rounded-full bg-muted px-1.5 py-0.5 text-[9px] font-medium text-muted-foreground">{result.type}</span></span><span className="mt-0.5 block truncate text-[10px] text-muted-foreground">{result.detail}</span></span></Link>) : <p className="p-4 text-center text-xs text-muted-foreground">No matching permitted records.</p>}</div></div></div>}
  </div>;
}
