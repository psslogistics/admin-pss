"use client";

import Link from "next/link";
import { ArrowUpRight, Check, Clock3, Ticket, UsersRound } from "lucide-react";
import { useEmployee } from "@/components/dashboard/employee-provider";

export default function EmployeeDashboard() {
  const { profile, tasks, getVisibleClients, getVisibleTickets, notifications, activity, can } = useEmployee();
  const clients = getVisibleClients(); const tickets = getVisibleTickets(); const openTickets = tickets.filter((ticket) => !["Resolved", "Closed"].includes(ticket.status)).length; const unread = notifications.filter((item) => !item.read).length;
  return <div className="space-y-6"><section className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm text-muted-foreground">Assigned employee workspace · {profile.workspace}</p><h1 className="mt-1 text-2xl font-semibold tracking-tight sm:text-3xl">Good morning, {profile.name.split(" ")[0]}</h1><p className="mt-2 max-w-2xl text-sm text-muted-foreground">Here is what needs your attention across your assigned clients today.</p></div><div className="flex items-center gap-2">{can("admin.tasks.view") && <Link prefetch={false} href="/dashboard/myTasks" className="inline-flex h-9 items-center gap-2 rounded-lg border border-border bg-card px-3 text-sm font-medium shadow-xs hover:bg-muted">View tasks</Link>}{can("admin.activity.view") && <Link prefetch={false} href="/dashboard/myActivity" className="inline-flex h-9 items-center gap-2 rounded-lg bg-primary px-3 text-sm font-medium text-primary-foreground shadow-xs hover:bg-primary/90">View activity <ArrowUpRight className="size-4" /></Link>}</div></section><section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Metric label="My tasks" value={String(tasks.length)} detail={`${tasks.filter((task) => task.status === "Pending").length} pending`} icon={Check} /><Metric label="In progress" value={String(tasks.filter((task) => task.status === "In progress").length)} detail="Active work" icon={Clock3} tone="warning" /><Metric label="Open tickets" value={String(openTickets)} detail="Assigned client queue" icon={Ticket} tone="warning" /><Metric label="Assigned clients" value={String(clients.length)} detail={`${unread} unread notifications`} icon={UsersRound} /></section><section className="grid gap-6 xl:grid-cols-[minmax(0,1.35fr)_minmax(320px,0.65fr)]"><section className="rounded-xl border border-border/70 bg-card shadow-xs"><div className="flex items-center justify-between border-b border-border/60 px-4 py-3 sm:px-5"><div><h2 className="text-sm font-semibold">Today&apos;s priorities</h2><p className="mt-0.5 text-xs text-muted-foreground">Your most important work at a glance</p></div>{can("admin.tasks.view") && <Link prefetch={false} href="/dashboard/myTasks" className="text-xs font-medium text-primary hover:underline">View all</Link>}</div><div className="divide-y divide-border/60">{tasks.slice(0, 4).map((task) => <div key={task.id} className="flex items-center gap-3 px-4 py-4 sm:px-5"><div className="grid size-8 shrink-0 place-items-center rounded-lg bg-primary/10 text-primary"><Check className="size-4" /></div><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{task.title}</p><p className="mt-1 truncate text-xs text-muted-foreground">{task.context} · {task.due}</p></div><span className="rounded-full bg-muted px-2 py-1 text-[10px] font-medium">{task.status}</span></div>)}</div></section><section className="rounded-xl border border-primary/20 bg-primary/[0.04] p-5"><p className="text-xs font-semibold uppercase tracking-widest text-primary">Workspace snapshot</p><h2 className="mt-3 text-lg font-semibold tracking-tight">Your client desk is ready.</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">You currently own the day-to-day relationship for {clients.length} clients. Tickets from those clients appear in your queue first.</p><Link prefetch={false} href="/dashboard/support" className="mt-4 inline-flex items-center gap-2 text-xs font-semibold text-primary hover:underline">Open support desk <ArrowUpRight className="size-3.5" /></Link></section></section><section className="rounded-xl border border-border/70 bg-card shadow-xs"><div className="flex items-center justify-between border-b border-border/60 px-4 py-3 sm:px-5"><div><h2 className="text-sm font-semibold">Recent activity</h2><p className="mt-0.5 text-xs text-muted-foreground">Your latest production activity</p></div>{can("admin.activity.view") && <Link prefetch={false} href="/dashboard/myActivity" className="text-xs font-medium text-primary hover:underline">See history</Link>}</div><div className="divide-y divide-border/60">{activity.slice(0, 5).map((item) => <div key={item.id} className="flex items-center gap-3 px-4 py-3.5 sm:px-5"><span className="grid size-8 shrink-0 place-items-center rounded-full bg-primary/10 text-primary"><Check className="size-3.5" /></span><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{item.title}</p><p className="truncate text-xs text-muted-foreground">{item.detail}</p></div><span className="shrink-0 text-[11px] text-muted-foreground">{item.time}</span></div>)}</div></section></div>;
}
function Metric({ label, value, detail, icon: Icon, tone = "normal" }: { label: string; value: string; detail: string; icon: typeof Check; tone?: "normal" | "warning" }) { return <article className="rounded-xl border border-border/70 bg-card p-4 shadow-xs"><div className={`grid size-9 place-items-center rounded-lg ${tone === "warning" ? "bg-amber-500/10 text-amber-600" : "bg-primary/10 text-primary"}`}><Icon className="size-4" /></div><p className="mt-4 text-2xl font-semibold tracking-tight">{value}</p><p className="mt-1 text-xs font-medium">{label}</p><p className="mt-1 text-[11px] text-muted-foreground">{detail}</p></article>; }
